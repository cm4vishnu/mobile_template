import React, { useEffect } from "react"; import { NotesRepository } from "./NotesRepository"; import { useToast } from "@/hooks/useToast"; import { useTheme } from "@/hooks/useTheme"; import { cn } from "@/lib/utils"; const NotesPage = () => { const { notes, loading, error, toast } = useNotes(); const { theme } = useTheme(); const { showSuccess, showError } = toast; const notesRepository = new NotesRepository(); useEffect(() => { // Initialize Firestore collection if not exists notesRepository.initialize(); }, []); // ... rest of the component code remains the same ... };