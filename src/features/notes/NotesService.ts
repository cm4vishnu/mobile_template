import { NotesRepository } from "./NotesRepository"; import { FrameworkError } from "@/utils/errors"; import { Note } from "@/types/notes"; export class NotesService { private readonly repository = new NotesRepository(); async getAll(): Promise<Note[]> { return this.repository.getAll(); } async create(note: Omit<Note, "id">): Promise<string> { return this.repository.create(note); } async update(id: string, updates: Partial<Note>): Promise<void> { return this.repository.update(id, updates); } async delete(id: string): Promise<void> { return this.repository.delete(id); } }